import { Link, Navigate, Route, Routes } from 'react-router-dom'
import { Jobs, SolutionPage, About } from './pages'
function Layout({ children }: { children: React.ReactNode }) { return <><header><Link className="brand" to="/">OUTLINED</Link><nav><Link to="/jobs">Job map</Link><Link to="/about">About</Link></nav></header><aside style={{ background: '#edf1de', borderBottom: '1px solid #d4d7ce', color: '#354039', fontSize: '.78rem', lineHeight: 1.55, margin: 'auto', maxWidth: 1240, padding: '13px 30px' }}><strong style={{ fontFamily: 'DM Mono, monospace', fontSize: '.7rem', letterSpacing: '.04em', marginRight: 8, textTransform: 'uppercase' }}>Companies, be aware — before any AI training can begin:</strong> Teach people to check original sources, recognise incentives and bias, compare evidence, and keep human judgement accountable. Search rankings, ads and AI outputs are not neutral truth.</aside><main>{children}</main><footer>OUTLINED · Independent public-job research and solution design.</footer></> }
export default function App() { return <Layout><Routes><Route path="/" element={<Navigate to="/jobs" replace />} /><Route path="/jobs" element={<Jobs />} /><Route path="/solutions/:slug" element={<SolutionPage />} /><Route path="/about" element={<About />} /><Route path="*" element={<Navigate to="/jobs" replace />} /></Routes></Layout> }
