import { describe, expect, it, vi } from 'vitest'
import worker from './index'
describe('worker safeguards', () => { it('rejects empty requests', async () => { const res = await worker.fetch(new Request('https://x/api/analyse-work', { method: 'POST', body: JSON.stringify({ solution_slug: 'abc-ai-adoption-specialist', input: {} }) }), { OPENAI_API_KEY: 'test' }); expect(res.status).toBe(400) }); it('rejects unknown solution slugs', async () => { const res = await worker.fetch(new Request('https://x/api/analyse-work', { method: 'POST', body: JSON.stringify({ solution_slug: 'missing', input: { task: 'test' } }) }), { OPENAI_API_KEY: 'test' }); expect(res.status).toBe(404) }); it('returns a clear upstream failure', async () => { vi.stubGlobal('fetch', vi.fn().mockRejectedValue(new Error('down'))); const res = await worker.fetch(new Request('https://x/api/analyse-work', { method: 'POST', body: JSON.stringify({ solution_slug: 'abc-ai-adoption-specialist', input: { task: 'test' } }) }), { OPENAI_API_KEY: 'test' }); expect(res.status).toBe(502); vi.unstubAllGlobals() }) })
