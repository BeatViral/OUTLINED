name = "outlined-api"
main = "worker/index.ts"
compatibility_date = "2026-08-25"

# Set with: wrangler secret put OPENAI_API_KEY
# The frontend is deployed to Cloudflare Pages; set VITE_API_BASE_URL to this Worker URL.
